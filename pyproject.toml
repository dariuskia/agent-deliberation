[project]
name = "agent-deliberation"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "openai>=1.0",
    "pyyaml>=6.0",
    "fastapi>=0.110",
    "uvicorn[standard]>=0.29",
    "sse-starlette>=2.0",
    "python-dotenv>=1.0",
    "wandb>=0.17",
]

[tool.setuptools]
packages = ["deliberation", "server"]
